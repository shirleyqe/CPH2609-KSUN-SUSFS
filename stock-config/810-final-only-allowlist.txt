# 810-CONTROL-V2 final-only allowlist.
#
# Entries: `CONFIG_X=value` asserts the EXACT value the final .config must
# carry (mismatch -> FAIL), `CONFIG_X` allows any y/m. Hashes start comments.
# In CONTROL-V2, all 7 vendor-injected control-only =y features plus OPLUS_CHG
# and OPLUS_FEATURE_SENSOR_CFG are explicitly disabled (=n) to match stock GKI
# behavior and eliminate runtime userspace conflicts (horae initcall, etc.).
CONFIG_CC_HAS_AUTO_VAR_INIT_ZERO_BARE=y   # toolchain probe (auto-var-init feature bit
     # of clang r450784e); no kernel code, benign noise.
CONFIG_OPLUS_SYSTEM_KERNEL=n   # explicitly disabled in CONTROL-V2
CONFIG_OPLUS_SYSTEM_KERNEL_QCOM=n   # explicitly disabled in CONTROL-V2
CONFIG_OPLUS_FEATURE_PERFORMANCE_SCHED=n   # explicitly disabled in CONTROL-V2
CONFIG_OPLUS_FEATURE_PERFORMANCE_MM=n   # explicitly disabled in CONTROL-V2
CONFIG_DYNAMIC_TUNING_SWAPPINESS=n   # explicitly disabled in CONTROL-V2
CONFIG_HORAE_THERMAL_SHELL=n   # explicitly disabled in CONTROL-V2
CONFIG_OPLUS_FEATURE_WINDOW_POLICY=n   # explicitly disabled in CONTROL-V2
CONFIG_OPLUS_CHG=n   # explicitly disabled in CONTROL-V2
CONFIG_OPLUS_FEATURE_SENSOR_CFG=n   # explicitly disabled in CONTROL-V2 (link fix)